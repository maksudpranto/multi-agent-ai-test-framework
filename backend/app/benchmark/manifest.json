[
  {
    "slug": "ipv4_validator",
    "title": "IPv4 address validator",
    "entrypoint": "is_valid_ipv4",
    "signature": "is_valid_ipv4(s: str) -> bool",
    "params": [
      {
        "name": "s",
        "type": "str",
        "note": "the string to validate"
      }
    ],
    "requirement": "As a form-validation feature, I want is_valid_ipv4(s) to return True when a\nstring is a well-formed IPv4 address and False otherwise.\n\nA well-formed address is four decimal numbers separated by dots, and each\nnumber is in the range 0 to 255. For example \"192.168.0.1\" is valid and\n\"10.0.0.256\" is not. The function returns a boolean; it never raises.",
    "canonical_inputs": [
      [
        "1.2.3.4"
      ],
      [
        "0.0.0.0"
      ],
      [
        "255.255.255.255"
      ],
      [
        "256.1.1.1"
      ],
      [
        "01.2.3.4"
      ],
      [
        "1.2.3.4.5"
      ],
      [
        "1.2.3"
      ],
      [
        "1.300.1.1"
      ],
      [
        "a.b.c.d"
      ]
    ],
    "reference_file": "fixtures/ipv4_validator/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "No upper-bound check, so octets above 255 are accepted.",
        "file": "fixtures/ipv4_validator/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "Leading-zero octets (e.g. '01') are wrongly accepted.",
        "file": "fixtures/ipv4_validator/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "Accepts 4 OR MORE parts (uses < instead of !=).",
        "file": "fixtures/ipv4_validator/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Boundary off-by-one: rejects exactly 255 (uses >= 255).",
        "file": "fixtures/ipv4_validator/mutants/m4.py"
      }
    ]
  },
  {
    "slug": "balanced_brackets",
    "title": "Balanced-brackets checker",
    "entrypoint": "is_balanced",
    "signature": "is_balanced(s: str) -> bool",
    "params": [
      {
        "name": "s",
        "type": "str",
        "note": "a string possibly containing brackets"
      }
    ],
    "requirement": "As a code-editor helper, I want is_balanced(s) to check whether the brackets\nin a string are balanced. The three bracket kinds are (), [] and {}.\n\nBrackets are balanced when every opening bracket is closed by a matching\nclosing bracket in the correct order, e.g. \"(a[b]{c})\" is balanced. Any\ncharacters that are not brackets are ignored. Returns a boolean.",
    "canonical_inputs": [
      [
        "()"
      ],
      [
        "([])"
      ],
      [
        "([)]"
      ],
      [
        "("
      ],
      [
        ")"
      ],
      [
        "(]"
      ],
      [
        ""
      ],
      [
        "a(b)c"
      ],
      [
        "())"
      ]
    ],
    "reference_file": "fixtures/balanced_brackets/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "Closing bracket type is not checked (pops without matching).",
        "file": "fixtures/balanced_brackets/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "Leftover unclosed brackets are ignored (always returns True at end).",
        "file": "fixtures/balanced_brackets/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "A closing bracket with nothing open is not treated as an error.",
        "file": "fixtures/balanced_brackets/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Only round brackets are recognised as openers.",
        "file": "fixtures/balanced_brackets/mutants/m4.py"
      }
    ]
  },
  {
    "slug": "slugify",
    "title": "URL slug generator",
    "entrypoint": "slugify",
    "signature": "slugify(s: str) -> str",
    "params": [
      {
        "name": "s",
        "type": "str",
        "note": "a title or phrase"
      }
    ],
    "requirement": "As a blog CMS, I want slugify(s) to turn a title into a URL slug.\n\nThe slug is lowercase, with words separated by single hyphens. Letters and\ndigits are kept; spaces, hyphens and underscores become hyphens; other\npunctuation is dropped. For example \"Hello, World!\" becomes \"hello-world\".\nThe result has no leading or trailing hyphen and no doubled hyphens.",
    "canonical_inputs": [
      [
        "Hello, World!"
      ],
      [
        "  Spaced  "
      ],
      [
        "Multiple   Spaces"
      ],
      [
        "Trailing-"
      ],
      [
        "ALL_CAPS"
      ],
      [
        ""
      ],
      [
        "a"
      ]
    ],
    "reference_file": "fixtures/slugify/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "Does not lowercase the input.",
        "file": "fixtures/slugify/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "Does not collapse repeated hyphens.",
        "file": "fixtures/slugify/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "Does not strip leading/trailing hyphens.",
        "file": "fixtures/slugify/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Underscores are dropped instead of becoming hyphens.",
        "file": "fixtures/slugify/mutants/m4.py"
      }
    ]
  },
  {
    "slug": "version_compare",
    "title": "Semantic-version comparator",
    "entrypoint": "compare_versions",
    "signature": "compare_versions(a: str, b: str) -> int",
    "params": [
      {
        "name": "a",
        "type": "str",
        "note": "a dotted version string, e.g. '1.2.0'"
      },
      {
        "name": "b",
        "type": "str",
        "note": "a dotted version string"
      }
    ],
    "requirement": "As a package manager, I want compare_versions(a, b) to compare two dotted\nversion strings and return -1 if a < b, 0 if they are equal, and 1 if a > b.\n\nEach component is compared numerically (so 1.10 is newer than 1.9). Missing\ntrailing components count as zero, so \"1.2\" and \"1.2.0\" are equal.",
    "canonical_inputs": [
      [
        "1.0.0",
        "1.0.0"
      ],
      [
        "1.2.0",
        "1.10.0"
      ],
      [
        "1.2",
        "1.2.0"
      ],
      [
        "1.0",
        "1.0.1"
      ],
      [
        "1.0.1",
        "1.0.2"
      ],
      [
        "2.0",
        "1.9"
      ]
    ],
    "reference_file": "fixtures/version_compare/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "Compares components as strings, so '2' > '10'.",
        "file": "fixtures/version_compare/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "No zero-padding, so '1.0' and '1.0.1' compare equal.",
        "file": "fixtures/version_compare/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "Sign inverted (returns 1 when a < b).",
        "file": "fixtures/version_compare/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Only the first two components are compared.",
        "file": "fixtures/version_compare/mutants/m4.py"
      }
    ]
  },
  {
    "slug": "caesar_cipher",
    "title": "Caesar cipher",
    "entrypoint": "caesar_cipher",
    "signature": "caesar_cipher(text: str, shift: int) -> str",
    "params": [
      {
        "name": "text",
        "type": "str",
        "note": "the text to encrypt"
      },
      {
        "name": "shift",
        "type": "int",
        "note": "how many letters to shift by"
      }
    ],
    "requirement": "As a puzzle app, I want caesar_cipher(text, shift) to shift each letter of\ntext forward through the alphabet by `shift` positions.\n\nLetters wrap around the end of the alphabet (z shifts to a), and the case of\neach letter is preserved. Characters that are not letters are left\nunchanged. For example caesar_cipher(\"Abc\", 1) returns \"Bcd\".",
    "canonical_inputs": [
      [
        "abc",
        1
      ],
      [
        "xyz",
        3
      ],
      [
        "ABC",
        1
      ],
      [
        "Hello, World!",
        5
      ],
      [
        "abc",
        0
      ],
      [
        "abc",
        27
      ]
    ],
    "reference_file": "fixtures/caesar_cipher/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "No wrap-around (drops the modulo), so 'z' overflows past 'z'.",
        "file": "fixtures/caesar_cipher/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "Uppercase letters are left unshifted (case not handled).",
        "file": "fixtures/caesar_cipher/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "Non-letters are shifted too, corrupting punctuation/spaces.",
        "file": "fixtures/caesar_cipher/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Wrap modulus is 25, not 26 \u2014 mishandles large shifts.",
        "file": "fixtures/caesar_cipher/mutants/m4.py"
      }
    ]
  },
  {
    "slug": "duration_parser",
    "title": "Duration parser",
    "entrypoint": "parse_duration",
    "signature": "parse_duration(s: str) -> int",
    "params": [
      {
        "name": "s",
        "type": "str",
        "note": "a duration like '1h30m'"
      }
    ],
    "requirement": "As a scheduling tool, I want parse_duration(s) to turn a short duration\nstring into a total number of seconds.\n\nThe string is made of number+unit pieces, where the unit is h (hours),\nm (minutes) or s (seconds), e.g. \"1h30m\" is 5400 and \"45s\" is 45. Pieces may\nbe combined in any mix. An empty string is 0.",
    "canonical_inputs": [
      [
        "1h"
      ],
      [
        "30m"
      ],
      [
        "45s"
      ],
      [
        "1h30m"
      ],
      [
        "2h15m30s"
      ],
      [
        ""
      ],
      [
        "90m"
      ]
    ],
    "reference_file": "fixtures/duration_parser/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "Hours use a 60x multiplier instead of 3600x.",
        "file": "fixtures/duration_parser/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "Seconds are ignored.",
        "file": "fixtures/duration_parser/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "Minutes are counted as seconds (no 60x).",
        "file": "fixtures/duration_parser/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Only the first piece is parsed; the rest is dropped.",
        "file": "fixtures/duration_parser/mutants/m4.py"
      }
    ]
  },
  {
    "slug": "rpn_calculator",
    "title": "RPN calculator",
    "entrypoint": "rpn_eval",
    "signature": "rpn_eval(expr: str) -> float",
    "params": [
      {
        "name": "expr",
        "type": "str",
        "note": "a space-separated RPN expression"
      }
    ],
    "requirement": "As a calculator engine, I want rpn_eval(expr) to evaluate a reverse-Polish\n(postfix) expression given as space-separated tokens and return the result\nas a float.\n\nTokens are numbers and the operators + - * /. Each operator pops the two\nmost recent values and applies itself, so \"10 2 -\" is 8 (the earlier value\nminus the later one) and \"3 4 +\" is 7. A lone number evaluates to itself.",
    "canonical_inputs": [
      [
        "3 4 +"
      ],
      [
        "10 2 -"
      ],
      [
        "2 3 *"
      ],
      [
        "8 2 /"
      ],
      [
        "5"
      ],
      [
        "1 2 - 3 *"
      ]
    ],
    "reference_file": "fixtures/rpn_calculator/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "Subtraction operands reversed (computes b - a).",
        "file": "fixtures/rpn_calculator/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "Division operands reversed (computes b / a).",
        "file": "fixtures/rpn_calculator/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "Multiplication is implemented as addition.",
        "file": "fixtures/rpn_calculator/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Division truncates to an int, losing the float result type.",
        "file": "fixtures/rpn_calculator/mutants/m4.py"
      }
    ]
  },
  {
    "slug": "run_length_encode",
    "title": "Run-length encoder",
    "entrypoint": "rle_encode",
    "signature": "rle_encode(s: str) -> str",
    "params": [
      {
        "name": "s",
        "type": "str",
        "note": "the string to encode"
      }
    ],
    "requirement": "As a compression utility, I want rle_encode(s) to run-length encode a string:\neach run of the same character becomes that character followed by the count\nof how many times it repeats.\n\nFor example \"aaabbc\" encodes to \"a3b2c1\". A single character becomes that\ncharacter followed by 1. The empty string encodes to the empty string.",
    "canonical_inputs": [
      [
        "aaabbc"
      ],
      [
        "a"
      ],
      [
        ""
      ],
      [
        "abc"
      ],
      [
        "aaaa"
      ],
      [
        "aabbbba"
      ]
    ],
    "reference_file": "fixtures/run_length_encode/reference.py",
    "mutants": [
      {
        "key": "m1",
        "description": "Forgets to emit the final run.",
        "file": "fixtures/run_length_encode/mutants/m1.py"
      },
      {
        "key": "m2",
        "description": "Run count starts at 0, so every count is one too low.",
        "file": "fixtures/run_length_encode/mutants/m2.py"
      },
      {
        "key": "m3",
        "description": "Empty input is not special-cased and crashes.",
        "file": "fixtures/run_length_encode/mutants/m3.py"
      },
      {
        "key": "m4",
        "description": "Count is not reset after a run ends.",
        "file": "fixtures/run_length_encode/mutants/m4.py"
      }
    ]
  }
]
