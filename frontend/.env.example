# Base URL of the backend API. Local dev defaults to http://localhost:8000.
# On Vercel, set this (Project → Settings → Environment Variables) to your
# deployed backend's public URL, e.g. https://matf-api.onrender.com
VITE_API_URL=http://localhost:8000
